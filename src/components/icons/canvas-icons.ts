/**
 * 画布图标模块 —— 唯一的一套线性图标
 *
 * 为什么不是一个 Vue 组件：
 *   菜单项里图标的取用方式有两种（`<path :d="opt.icon" />` 和 `<component :is>`），
 *   如果图标是组件，纯数据模块（比如 node-suggestions 的展示信息表）就没法引用它。
 *   所以这里只存**路径数据**，由使用方决定怎么渲染、用什么描边。
 *
 * 统一的画法（所有图标必须一致，新增图标也要守）：
 *   画布 24×24；几何尺寸控制在 3.5–20.5 之间，留出统一的视觉边距；
 *   描边 1.5、圆头圆角（stroke-linecap/linejoin: round）、fill: none；
 *   颜色一律 currentColor，模块内不出现任何色值 —— 颜色由使用方决定。
 *   圆弧的各个参数之间一定留空格（写 `0 0 1 x y`，不写 `001x y`），
 *   坐标也要用空格或逗号隔开 —— 紧挨着写虽然合法，但按数字切词的工具会读错。
 *
 * 取用方式：
 *   getCanvasIcon('image')            → 路径 d 字符串，直接喂给 <path :d>
 *   CANVAS_ICONS.image                → 同上（需要静态索引时）
 *   CANVAS_ICON_NAMES                 → 全部名字，可用来做遍历 / 校验
 *
 * 路径全部为本项目手绘的原创几何形状，没有引用任何第三方图标库的 path 数据。
 */

/** 全部图标名。类型从这个数组推导，保证「名字」和「数据」不会各写一份走偏。 */
export const CANVAS_ICON_NAMES = [
  'text',
  'image',
  'video',
  'llm',
  'upload',
  'duplicate',
  'delete',
  'download',
  'settings',
  'plus',
  'folder',
  'magic',
  'layers',
] as const

export type CanavasIconName = (typeof CANVAS_ICON_NAMES)[number]

/**
 * 图标名 → SVG path 的 `d`。
 * 全部按 24×24 画布绘制，多段形状用多个 `M` 子路径拼在同一个字符串里，
 * 这样调用方永远只需要一个 `<path>`。
 */
export const CANVAS_ICONS: Record<CanavasIconName, string> = {
  // 三行文本，中间一行收短，避免和「三横线」类图标混淆
  text: 'M5 6.5h14M5 12h9M5 17.5h13',

  // 相框 + 山峰 + 太阳
  image:
    'M5 5.5h14a1.5 1.5 0 0 1 1.5 1.5v10a1.5 1.5 0 0 1 -1.5 1.5H5a1.5 1.5 0 0 1 -1.5 -1.5V7A1.5 1.5 0 0 1 5 5.5z' +
    'M4 15.2l3.8 -3.8 3.4 3.4 2.5 -2.5 5.3 5.3' +
    'M10.1 9.1a1.15 1.15 0 0 1 -2.3 0a1.15 1.15 0 0 1 2.3 0',

  // 相框 + 播放三角（和 image 共用同一个外框，两张卡片摆一起时视觉重量一致）
  video:
    'M5 5.5h14a1.5 1.5 0 0 1 1.5 1.5v10a1.5 1.5 0 0 1 -1.5 1.5H5a1.5 1.5 0 0 1 -1.5 -1.5V7A1.5 1.5 0 0 1 5 5.5z' +
    'M10 9.3l4.4 2.7 -4.4 2.7z',

  // 对话气泡 + 两行内容：LLM 节点的产物是文本，气泡比「芯片」更直白
  llm:
    'M4.5 7A1.5 1.5 0 0 1 6 5.5h12A1.5 1.5 0 0 1 19.5 7v7a1.5 1.5 0 0 1 -1.5 1.5h-5.6L9 19.5v-4H6A1.5 1.5 0 0 1 4.5 14z' +
    'M7.5 9h9M7.5 12h5.5',

  // 向上箭头 + 承接托盘
  upload: 'M12 15.5V4.5M7.5 9L12 4.5 16.5 9M4.5 16.5v2a1.5 1.5 0 0 0 1.5 1.5h12a1.5 1.5 0 0 0 1.5 -1.5v-2',

  // 前片完整、后片只露右上两边的两个错位方框
  duplicate:
    'M8 5.5h8a1.5 1.5 0 0 1 1.5 1.5v8' +
    'M5.5 8.5h9a1.5 1.5 0 0 1 1.5 1.5v9a1.5 1.5 0 0 1 -1.5 1.5h-9A1.5 1.5 0 0 1 4 19v-9a1.5 1.5 0 0 1 1.5 -1.5z',

  // 垃圾桶：盖沿 + 提手 + 桶身 + 两道竖纹
  delete:
    'M4.5 7.5h15M9.5 7.5V5.5a1 1 0 0 1 1 -1h3a1 1 0 0 1 1 1v2' +
    'M6.8 7.5l0.9 11.2A1.5 1.5 0 0 0 9.2 20h5.6a1.5 1.5 0 0 0 1.5 -1.3l0.9 -11.2' +
    'M10.5 11v5.5M13.5 11v5.5',

  // 和 upload 镜像：向下箭头 + 承接托盘
  download: 'M12 4.5v11M7.5 11l4.5 4.5 4.5 -4.5M4.5 16.5v2a1.5 1.5 0 0 0 1.5 1.5h12a1.5 1.5 0 0 0 1.5 -1.5v-2',

  // 两条带滑块的滑杆：设置 = 可调项，比齿轮更容易在 16px 下看清
  settings:
    'M4 8h9.5M17.5 8H20M4 16h2.5M10.5 16H20' +
    'M13.5 8a1.5 1.5 0 1 1 3 0a1.5 1.5 0 1 1 -3 0' +
    'M7.5 16a1.5 1.5 0 1 1 3 0a1.5 1.5 0 1 1 -3 0',

  // 十字加号
  plus: 'M12 5v14M5 12h14',

  // 带标签页的文件夹
  folder:
    'M4 8a1.5 1.5 0 0 1 1.5 -1.5h3.3a1.5 1.5 0 0 1 1.2 0.6l1 1.4h7.5A1.5 1.5 0 0 1 20 10v7.5a1.5 1.5 0 0 1 -1.5 1.5h-13A1.5 1.5 0 0 1 4 17.5z',

  // 斜握的魔棒 + 两枚大小不同的四角星（AI 生成 / 刷子）
  magic: 'M4.5 19.5l10 -10M16.2 3.6v3.6M14.4 5.4h3.6M19 10.8v2.4M17.8 12h2.4',

  // 三层叠片：顶层是完整菱形，下面两层只画下沿
  layers: 'M12 3.6l8.4 4.1 -8.4 4.1 -8.4 -4.1zM3.6 11.6l8.4 4.1 8.4 -4.1M3.6 16l8.4 4.1 8.4 -4.1',
}

/**
 * 按名字取图标路径。
 * 取不到时返回空串而不是 undefined —— 调用方直接 `:d="getCanvasIcon(x)"` 不会渲染出 "undefined"。
 */
export const getCanvasIcon = (name: CanavasIconName): string => CANVAS_ICONS[name] || ''
