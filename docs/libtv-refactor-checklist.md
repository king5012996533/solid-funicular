# LibTV 功能对齐改造清单

> 这份清单是**施工队列**，不是设计文档。每一项都能独立开工、独立验收。
>
> **证据来源**（每条结论都可追溯）：
> - `[实测]` = 我登录后在其线上画布逐项点开抓下来的原文（DOM 文案 / 面板结构）
> - `[公告]` = LibTV 官方公开更新记录 `liblib.tv/feature-updates`（v1.5 / v1.0）的原文描述
> - `[现状]` = 我们仓库里实际的代码与实测行为
>
> **依赖标注**：
> - `前端` = 纯前端可完成，不依赖后端改动，也不依赖第三方 Key
> - `后端` = 需要新的服务端执行策略（现有策略只有 `image` / `agent-chat` / `agent-workspace` / `research-report`）
> - `Key` = 需要上游 API Key（当前 `ai_provider_configs` 为空表，图片生成实测已打到火山方舟并返回 `AuthenticationError`）
>
> **当前基线**：单测 9 个文件 287 项；浏览器 e2e 4 个脚本 84 项。每次改动后这些必须保持全绿。

---

## 1. 现状对照总表

| 能力 | LibTV | 我们 | 差距类型 |
|---|---|---|---|
| 节点类型 | **8 类** + 素材库（风格库/特效库） | 4 类（text/image/video/llmConfig） | 前端 + 后端 |
| 节点标题 | 卡片上方，**带 ▶ 折叠** | 卡片上方，无折叠 | 前端 |
| 输入需求声明 | 按类型说清（「请连接视频节点后操作」） | ✅ 刚做（`node-input-rules.ts`） | — |
| 每类节点的能力预设 | 各有 2–4 项「尝试」 | ✅ 有，但语义混杂（把"下一跳"和"本节点能力"混在一起） | 前端 |
| 参数集 | **按类型分开**（图片：比例·画质·分辨率·张数 / 视频：比例·分辨率·时长·个数 / 剪辑：无"个数"） | 按模型能力走；视频缺「数量」 | 前端 |
| composer 快捷 chip | 参考 / 标记 / 特效 / 角色库 / 运镜（视频 5 个） | 无 | 前端 |
| 节点开关 | 联网搜索 / 自动校验素材 / 智能引用 AutoLink | 无 | 前端（AutoLink 需后端配合） |
| @ 素材引用 | 两级面板，**内联 chip** | ✅ 两级面板，但是**纯文本 token** | 前端 |
| 智能引用 AutoLink | 自动把参考素材 @ 进提示词 | 相当于常开，但**不往提示词里写 token** | 前端 |
| 图片生成 | 通 | ✅ 链路通，**卡在 Key** | Key |
| 视频生成 | 通（Seedance 2.5，>5min 直出） | ❌ **服务端没有 video 策略** | 后端 |
| 音频生成 | 有节点 | ❌ 无节点类型、无策略 | 后端 |
| 剧本生成 | 有节点（新旧两版） | ❌ 无 | 后端（可复用 agent-chat） |
| 智能剪辑 | 有节点，4 个场景 | ❌ 无 | 后端 |
| 逐帧拉片 | 有节点，3 个拆解维度 | ❌ 无 | 后端 |
| 导演台（3D） | 有节点 | ❌ 无 | 后端 + 3D 模块 |
| 角色造型室 | 有（画布工具条入口 + 节点） | ❌ 无（9 个「接入中」里最接近的也只是占位） | 后端 |
| 资产管理 | 画布节点 + 底部入口 | 有资产页，**不是画布节点** | 前端 |
| Agent 操作节点 | TV Director：新建 / 更新 / 运行画布节点，可切自动/手动 | ❌ 无 | 后端 |
| 画布性能 | 数千节点不卡 | 未知（未压测） | 前端 |

---

## 2. 第一阶段：纯前端（不依赖后端、不依赖 Key）

> 这一阶段可以立刻开工、立刻验证。**建议全部做完再动后端**，因为它们同时是后续功能的容器。

### F1. 节点折叠 `前端` ✅
- **依据**：`[实测]` 节点标题前有 `▶`，展开/收起卡片主体
- **做法**：节点 data 加 `collapsed?: boolean`；折叠时卡片只留标题 + 一行参数摘要；折叠态高度固定（如 40px）；折叠不改变连线锚点位置
- **涉及**：`useWorkflowCanvas.ts`（data 类型 + 默认值）、四个节点组件、`workflow.css`
- **验收**：单测覆盖折叠态的高度计算；浏览器实测折叠/展开后连线不断、位置不跳
- **✅ 已完成**（2026-09-21）。三个节点类型（文本/图片/视频）都接上了：
  标题图标**之前**加 `▾` 开关（与 LibTV 位置一致），折叠后卡片收到 34px、
  只留一行摘要（图片/视频显示输入状态、文本显示正文开头）。
  折叠状态存在节点 data 上（能保存、能撤销、复制节点会带走）。
  实测：卡片 224→34px、连线终点 300→181 自动重新锚定、重载后仍保持折叠。
- 实测中发现并修掉一个 CSS bug：折叠摘要超长时**被硬切、没有省略号** ——
  flex 子元素显示 `text-overflow: ellipsis` 需要先有 `min-width: 0`
  （flex 项默认 `min-width: auto` 会被内容撑开）。补上后正常显示省略号。

### F2. 参数集按节点类型分开 `前端` ✅
- **依据**：`[实测]` 图片 `16:9 · 标准画质 · 2K · 1张`；视频 `16:9 · 720P · 5s · 1个`；剪辑 `16:9 · 720P · 30s`（**没有"个数"**）
- **做法**：`model-params.ts` 里的 schema 增加「节点类型维度」——同一个模型在图片节点暴露尺寸/画质/张数，在视频节点暴露比例/分辨率/时长/个数；视频补上「数量」控件（受 `capabilityJson.maxCount` 约束，默认 1 时不显示）
- **涉及**：`config/model-params.ts`、`toolbars/VideoToolbar.vue`、`scripts/seed-model-catalog.mjs`（给视频模型声明 maxCount）
- **验收**：单测断言三类节点的参数维度集合不同；浏览器实测视频节点出现「数量」步进器

### F3. composer 快捷 chip 行 `前端` ✅
- **依据**：`[实测]` 图片节点 composer 上方有 `+参考 / 标记 / 风格`；视频节点是 `参考 / 标记 / 特效 / 角色库 / 运镜`
- **做法**：在 `ContentGenerator` 里加一行可配置的能力 chip（由节点类型决定显示哪些）。**每一项必须有真实行为**，没有实现的不摆（参考 `ImageNode` 里 9 个「接入中」的教训）
  - `参考` → 就是现在的 @ 素材引用入口（已有）
  - `风格` → 预置风格提示词模板（前端可做：往提示词里插模板文本）
  - `标记` / `特效` / `角色库` / `运镜` → **先不做**，等后端能力到位
- **涉及**：`ContentGenerator.vue`、新增 chip 配置表
- **验收**：浏览器实测 chip 行渲染、点击有实际效果（不是 toast）
- **✅ 已完成**（2026-09-21）。实际只摆了 2 个：`参考`（打开 @ 素材面板）
  与 `风格`（6 个风格片段，点一次追加、再点移除，chip 显示已用数量角标）。
  `标记 / 特效 / 角色库 / 运镜` 需要后端能力，按"宁缺勿假"没有摆。

### F4. 节点开关 `前端` ✅
- **依据**：`[实测]` 视频节点底部有 `高级设置 / 联网搜索 / 自动校验素材 / 智能引用 AutoLink`
- **做法**：`ContentGenerator` 底部加开关区，按类型显示：
  - `高级设置` → 已有（`AdvancedParamsPopover`）
  - `自动校验素材` → **前端可做**：提交前校验参考图格式/可访问性，不可用就拦下并提示
  - `联网搜索` / `智能引用 AutoLink` → 需要后端能力，**先不摆**
- **涉及**：`ContentGenerator.vue`
- **验收**：提交非法参考图时被拦下且有明确提示
- **✅ 已完成**（2026-09-21）。实现过程中踩到一个**会伤害用户的 bug**，
  被 e2e 抓出来：最初的可访问性探测先发 HEAD、只在 405/501 时回落 GET，
  而这个项目的静态文件处理器**对 HEAD 返回 404**（GET 是 200）——
  结果是每一张正常的上传图都被判成"取不到"，把正常提交全拦死。
  改用 `new Image()` 探测后解决，顺带带来三个好处：判据与界面一致、
  不依赖服务端支持 HEAD、不需要 CORS（否则会出现"界面能显示、校验说不行"）。
- 顺带收口了一处重复：同样的栅格格式过滤在 `nodes/ImageNode.vue` 与
  `canana/RightPanel.vue` 里各写了一遍且行为不一致（后者漏了 `data:image/svg`），
  现在统一到 `config/reference-validation.ts`。

### F5. 节点能力预设与「下一跳」分离 `前端` ✅
- **依据**：`[实测]` LibTV 节点内的「尝试：」是**本节点的操作模式**（智能剪辑给 4 种剪辑场景）；"下一跳"是 ⊕ 手柄 + 全局添加节点面板，两件事不混
- **现状问题**：我们图片节点的「尝试」里混了 `图生视频` / `首帧图生视频` —— 那是**创建下游节点**，不是本节点能力
- **做法**：把「尝试」拆成两组，或把"创建下游"移回 ⊕ 手柄；保证每一组语义单一
- **涉及**：`ImageNode.vue` / `VideoNode.vue`
- **验收**：浏览器实测两组入口各自可达、行为符合语义
- **✅ 已完成**（2026-09-21）。实际动手时发现比我原先写的更严重：
  图片节点的 4 项里**没有一项是本节点能力** —— 图生图/图生视频/首帧图生视频
  都是"创建下游节点"（而 ⊕ 手柄那条路径已经覆盖了），「图片换背景」只弹
  「接入中」。视频节点的 2 项（全能参考/首尾帧生视频）也全是假的。
  最终做法：图片节点**不再摆「尝试」**（它目前没有可供选择的模式 ——
  图生图/文生图由有没有参考图自动决定），只留标注清楚的「生成下游节点：」
  三项；视频节点把假项换成模型声明的**真实生成方式**（点击写 `feature` 到节点
  data，经 `initialParams` 同步给工具栏），并给当前生效项加高亮。

### F6. 资产管理作为画布节点 `前端` ✅
- **依据**：`[实测]` `添加节点` 面板有「素材库 ▸ 风格库 / 特效库」；底部工具条有「资产管理」
- **做法**：新增 `asset` 节点类型，渲染资产库（复用现有资产页组件），可作为参考图来源接进下游
- **涉及**：`WorkflowNodeType`、`node-suggestions.ts`（候选规则）、新节点组件
- **验收**：单测覆盖新类型的候选规则；浏览器实测能建、能连、能当参考图
- **✅ 已完成**（2026-09-21）。新增 `asset` 节点类型 + `AssetNode.vue`：
  从素材库选一个素材、或上传本地图，选中后写 `url` 到节点 data，
  下游按图片（或视频）素材消费 —— 也就是「这张参考图从哪来」在画布上可见了。
- 配套改动：`WorkflowNodeType` 加 `asset`、`NODE_INPUT_SPECS.asset`、
  `readUpstreamKind` 认素材节点、`COHERENT_DOWNSTREAM.asset = ['image','video']`、
  画布注册、尺寸约束。加完之后**三处枚举类测试同时失败**（断言"恰好四类节点"），
  这正是它们该做的事 —— 逼着改动者显式更新契约。已更新为五类。
- 实测：双击空白 → 建出素材节点（自动带上 F1 的折叠开关）；
  空态两个入口；素材库面板能打开。当前 `asset_items` 表是空的，
  所以面板显示「素材库还是空的」—— 这是真实状态，不是加载失败。

### F7. 画布快捷键与工具条补齐 `前端` ✅
- **依据**：`[实测]` 底部工具条：`添加节点 / 移动 / 素材库 / 角色造型室 / 生成历史 / 快捷键 / 教程`；左下：`资产管理 / 整理画布(Opt+Shift+F) / 切换小地图 / 隐藏节点连线 / 网格吸附 / 缩放`
- **做法**：对齐我们已有的对应项（整理画布 = auto-layout？我们是手动；隐藏连线、网格吸附我们已有），补「快捷键」面板与「教程」入口
- **涉及**：`index.vue`、`workflow.css`
- **验收**：浏览器实测每个按钮都有行为
- **✅ 已完成**（2026-09-21）。按「宁缺勿假」只补了有真实行为的三项：
  1. **整理画布**（新增 `config/canvas-layout.ts`，纯函数 + 25 项单测）。
     用**分层布局**而不是网格：按「离根的层数」分列（取**最长**路径，
     这样多上游的节点一定落在所有上游右边，不会出现连线往左指）；
     列内按节点**当前**纵坐标排序 —— 保留用户手动调整过的相对顺序，
     整理只解决"乱"、不该重排意图。快捷键 `⌥⇧F` 与 LibTV 同一个组合键。
  2. **快捷键面板**：列的是我们**实际注册**的快捷键（Esc / ⌘Z / ⌘⇧Z / ⌘N /
     ⌘A / ⌘C / ⌘V / Delete / Space 平移，加上自己的双击建节点、⊕ 拖线），
     分画布 / 节点 / 编辑三组。不是抄 LibTV 的清单。
  3. **素材库**入口：直接建一个素材节点（复用刚做的 F6）。
- **没做「教程」** —— 我们没有教程内容，摆一个点开是空白的入口不如不摆。
- 实测：整理画布把拖乱的画布重排成左右两列且流向正确
  （拖乱后 `[[228,178],[580,370]]` → 整理后 `[[120,280],[750,220]]`）；
  快捷键面板 3 组 13 行；工具条 10 个按钮全部有行为。
- 写测试时抓到我**自己的一个错误期望**：纯环（A→B→C→A）我原以为会降级成单列，
  实际代码把它线性化成三列 —— 后者明显更好（单列会把节点叠在一起、反而看不出结构）。
  已改正期望并把这个判断写进注释。

### F8. @ 引用升级为内联 chip `前端` ✅
- **依据**：`[实测]` LibTV 的 @ 是内联 chip（带缩略图），不是纯文本
- **现状**：我们已完成 token + 解析层（`reference-resolver.ts`），**数据模型可直接复用**
- **做法**：把 `<textarea>` 换成 `contenteditable`，token 渲染成 chip；**必须处理中文 IME**（`compositionstart/end`）
- **涉及**：`ContentGenerator.vue`、`MentionPicker.vue`
- **风险**：这是本清单里前端风险最高的一项（输入法、粘贴、Enter 发送、光标位置、placeholder 全部要重做）
- **验收**：浏览器实测中文输入法组字不打断、chip 可增删、提交载荷与现在一致
- **✅ 已完成**（2026-09-21）。新增 `InlineMentionInput.vue`：受控的 contenteditable，
  **纯文本仍是唯一真相**，DOM 只是它的投影 —— 输入时把 DOM 序列化回 `@图片1`
  emit 出去，所以 `resolve-reference` 那层一行没动。已插 token 渲染成
  `contenteditable=false` 的 chip。仅在画布节点那两类 composer 启用；
  其余 13 个调用方仍是原生 textarea，行为不变。
- **IME 的三条硬约束**：① composition 期间不读也不写 DOM；
  ② `compositionend` 后补一次序列化（部分输入法最后那次 `input` 仍带 isComposing）；
  ③ 重渲染后按**纯文本偏移**还原光标（chip 是原子节点，按 DOM 偏移会偏一位）。
- **IME 验证到什么程度**：用 CDP `Input.imeSetComposition` 走浏览器真实合成管线
  （不是合成 JS 事件），确认组字期间 chip 未被重建、上屏后文本正确、
  组字中的 Enter 被当成上屏。**没验证**：真实输入法的候选窗/翻页/选词路径、
  Safari 与 Firefox 的 IME、组字起点紧贴 chip 的边界 —— headless + CDP 不是真正的输入法。
- **用户可察觉的行为差异**（相对旧 textarea）：chip 是整体、Backspace 一次删掉整个引用；
  手敲的 token 要等一个分隔符才收成 chip（为了 `@图片1` 能续着敲成 `@图片12`）；
  粘贴一律按纯文本；Cmd+Z 走浏览器原生 contenteditable 撤销栈，粒度比 textarea 粗；
  输入框自动增高（96 → 上限 40vh）。
- 顺带修了一处**只有跑起来才发现**的问题：chip 是 `document.createElement` 建的，
  拿不到 Vue 给模板节点附加的 scope 属性，所以 chip 的样式必须写成非 scoped，
  否则 chip 完全没有样式（实测当时是白字透明底）。

### F9. 画布性能压测 `前端` ✅
- **依据**：`[公告]` v1.5「优化大量节点的画布（数千节点）卡顿体验问题」「修复画布偶尔丢失节点的问题」
- **做法**：造 1000 / 3000 节点画布，测拖拽帧率与首屏时间；若卡，按需加虚拟化
- **涉及**：`index.vue` / `useWorkflowCanvas.ts`
- **验收**：给出实测数字（帧率 / 时间），不接受"感觉还行"
- **✅ 已完成**（2026-09-21）。新增可复现的压测脚本 `tests/e2e/canvas-perf.mjs`
  （直接造夹具写库 → 加载 → 量首屏与拖拽帧率 → 自动清理）。

**实测数字**（1600×1000 视口，真实鼠标拖拽 60 步）：

| 节点数 | 视口虚拟化 | 渲染稳定 | 拖拽帧率 |
|---|---|---|---|
| 100 | 关 | 1693 ms | **58.9 FPS** |
| 300 | 关 | 3119 ms | **60.6 FPS** |
| 300 | 开 | 1585 ms | 37.7 FPS |
| 1000 | 关 | 8302 ms | 24.1 FPS |
| 1000 | 开 | 3079 ms | 15.2 FPS |

**结论（与直觉相反，以数据为准）**
1. **300 节点以内是满帧的**，不用优化。
2. **视口虚拟化（`onlyRenderVisibleElements`）持续伤害拖拽帧率**
   （300 节点 60.6 → 37.7），只换来加载时间减半。原因是拖拽时每帧都要重算
   可见集合并频繁增删 DOM，比"保留全部节点、只移动一个"更贵。
   **所以没有启用它** —— 这个产品的主交互就是拖节点。
3. 1000 节点两种配置都不合格（24 / 15 FPS），真正的原因是**节点组件太重**
   （每个都渲染工具条、图标、子菜单），不是图遍历。

**已经做的优化**：把「每个节点/边各自遍历整个 edges/nodes」改成共享索引
（`composables/workflow-graph-index.ts`）。原先 1000 节点下是 O(N×E) ——
950 条边组件 × 每条扫 950 条 ≈ 每帧 90 万次操作。索引化后每帧只重建一次。
**诚实地说：这个改动只把首屏从 2319ms 降到 1859ms，帧率基本没动**
（24.3 → 24.1）。最初我根据"去掉边会快 5.6 倍"的对照组推断瓶颈在边遍历，
那是错的 —— 快是因为少渲染 950 个 SVG 边组件，不是因为少遍历。

详见 **F9b**。

---

### F9b. 画布性能根治：拿掉每帧快照与每帧警告 `前端` ✅
- **✅ 已完成**（2026-09-21）。**这一项我前后猜错三次，最后靠 profiler 才找对**，
  所以把过程一并记下来 —— 免得以后又从"看起来最可疑的地方"下手。
- **三次错误假设**：
  1. F9 时按「去掉边会快 5.6 倍」推断瓶颈在**边遍历** → 索引化后帧率没变（错）
  2. F9b 时推断是**节点卡片内容太重** → 把全部节点折叠（卡片内容几乎不渲染）
     后帧率**一点没变**（错）
  3. 看到 profile 里 `warn$1` 占 22% 时，怀疑是 `selectionKeyCode` 的 prop
     校验警告 → A/B 把它换成合法值后热点纹丝不动（错）
- **改用 profiler 找到的两个真凶**（新增 `tests/e2e/profile-drag.mjs`：
  用 CDP 抓 V8 CPU profile，按函数聚合自耗时排行）：

  **① 每帧 `JSON.stringify` 整张画布（占拖拽 CPU 的 50.9%）**
  `currentCanvasSnapshot` 是个 computed，用来判断「相对上次保存有没有变化」，
  而它 `JSON.stringify(nodes.value)` 会读遍每个节点的每个属性 —— 于是拖拽时
  position 每帧变化都会让它失效、下一帧整张画布重新序列化 + 多 MB 字符串比对。
  改成**防抖结算**（400ms）：判断"要不要保存"不需要帧级精度，用户看不出这 400ms。

  **② 每帧一次的 Vue 警告（占 22%）**
  绕开控制台去重、直接计数后发现：拖拽 2 秒内触发 **977 次**
  `[Vue warn]: Extraneous non-props attributes (sourceNode, targetNode, type, updatable …)`。
  原因是自定义边组件没声明 Vue Flow 传进来的那些内部属性，Vue 就把它们当外部属性
  处理、每次渲染都警告 —— 而 `warn` 内部会调 `getComponentTrace()` 遍历组件栈。
  在四个边组件上加 `inheritAttrs: false`（明确「不转发未知属性」）。
  **DOM 本身一直是干净的**，纯粹是警告的开销。

- **实测效果**（1000 节点，真实鼠标拖拽 60 步）：

  | 指标 | 原始 | 修①后 | 修②后 |
  |---|---|---|---|
  | 拖拽帧率 | 24.2 FPS | 42.9 | **53.7 FPS** |
  | 渲染稳定 | 8541 ms | 3799 | **2151 ms** |
  | CPU 采样数 | 20073 | 7592 | **5511** |
  | `warn$1` 热点 | — | 22.6% | **消失** |

  300 节点 60.4 FPS（基线 60.6，在噪声内，不算退化）。
- **行为变更已单独验证**：快照防抖会影响 autosave 触发时机，实测
  「加两个节点 → 等防抖 → 库里节点数与 DOM 一致」（2 → 4，DOM 4）。
- **顺带修好测试基建的一处误判**：巡检类脚本的「无 console error」断言会因为
  **第三方热链资源**解析失败而误报（代理一开，首页 32 张 `qwe-oss` 的图全裂成
  `ERR_NAME_NOT_RESOLVED`）。抽出 `tests/e2e/lib/console-filters.mjs`，
  把「我们自己的报错」与「第三方资源失败」分开：前者判失败，后者只统计并报出来。
  **不隐藏** —— 它本身就是下面 §5 记的真实风险。

## 3. 第二阶段：需要后端策略（不需要第三方 Key）

> 每项 = 新执行策略 + 任务轮询 + 结算。`image` 策略是现成模板，照着做。

### 3.0 先决条件：中转站的实际能力边界（2026-09-21 实测）

这一节是 B1~B3 能不能做的前提。**结论：当前这把 Key 只能出图，视频和对话都在账号层被挡住，
不是代码问题。** 写清楚是为了以后不重复排查。

**抓法**（比试路径可靠）：中转站是 new-api，`GET /api/pricing` 会逐模型返回
`supported_endpoint_types` 与 `enable_groups`，这才是权威答案。

**Key 实际落在哪个组**：`gpt-绘画4k专用`。该组下**一共 11 个模型，全是图像**：

| 模型 | 端点类型 | 价格 |
| --- | --- | --- |
| `gpt-image-2` | openai / image-generation / image-edits | $0.03 |
| `gpt-image-2.5`（**已接入**） | openai / image-generation / image-edits | $0.04 |
| `gpt-image-2.5` / `-flare` / `-sunburst` | openai / image-generation / image-edits | $0.04~0.05 |
| `gemini-3-pro-image-preview`(-c) | gemini / openai | ratio 榜 |
| `gemini-3.1-flash-image-preview`(-c) | gemini / openai | $0.08~0.15 |
| `grok-imagine-image-2.0` / `-quality` | openai / openai-response | $0.035~0.08 |
| `grok-imagine-video-1.5` | openai / openai-response | $0.20 |

**视频不可用的证据**：全站只有 `grok-imagine-0.9` 声明了 `openai-videos` + `查询视频`
（也就是中转站真正铺了视频路由的那个），它**不在本组**：

```
POST /v1/video/generations  {"model":"grok-imagine-0.9"}
→ 503 No available channel for model grok-imagine-0.9 under group gpt-绘画4k专用
```

而本组里那个 `grok-imagine-video-1.5` 虽然名字像视频模型，但没铺视频路由
（`invalid api platform: 48`），走 `chat/completions` 是 `502 openai_error`、
走 `responses` 是上游直接拒。**它在 /v1/models 里出现，不代表能用。**

**对话不可用**：`gemini-3-pro-preview` 等一并 `No available channel ... under group
gpt-绘画4k专用` —— 该组没有任何 chat 模型。

**要解锁需要**：在中转站后台新建一个属于 `AI视频`（视频）或 `a全模型综合组-1倍率`（对话）
分组的令牌。分组是绑在令牌上的，不是绑在账号上的，所以改 Token 即可，不用改代码。
拿到新 Key 后只需在后台「厂商配置」里新增一个厂商，B1/B3 就能接。

**新增对话厂商：通义千问 MaaS**（2026-09-22，用户提供）
`https://token-plan.maas.qianwenaiapi.com/compatible-mode/v1`（OpenAI 兼容）。
这是本项目**第一个可用的对话模型** —— 之前 ggwk1 那把 Key 的分组里只有图像模型，
所以文本 / LLM 节点 / Agent / 研究搜索（`grok2api` 策略需要 modelKey）全都是死的。

- 已登记：厂商 `qwen-maas`，模型 `qwen3.8-flash`（`category=CHAT`，provider 的
  `defaultChatModel` 指向它 → 目录 `defaults.chat` 自动指向它）。
- 实测：`/models` 列出 **15 个模型**（对话：qwen3.8-flash/max、glm-5.2/5.3、
  deepseek-v4-pro 等；图片：`wan2.7-image` / `-pro`；语音：`qwen-audio-3.0-tts-plus` /
  `-realtime-plus`）。**但只有 chat 能调用** —— 见下面 B2 那段的判别实验。
- 端到端实测（`tests/e2e/submit-chat.mjs`）：目录解析出 providerId → 建任务 →
  3 秒完成 → 模型回「链路正常」。浏览器里 LLM 节点的模型下拉默认即 `Qwen3.8 Flash`。
- 注意它是**思考型模型**（usage 里有 `reasoning_tokens`，简单问答也先推理），
  所以首字延迟比普通模型高。

> **纠正一处我自己写错的话**：上一轮我按 `/models` 的返回写了「图片与语音那两个
> 对电商出图 / 漫剧配音有用，值得后面用上」。**这是错的** —— 这个网关只转发
> `chat/completions`（外加 `/models`），图像与语音的路由都不存在。
> 「`/models` 列出了」≠「能调用」，这跟 `grok-imagine-video-1.5` 是同一个坑，
> 第二次踩了才学会：下结论前必须做一次**与模型无关的对照实验**（见 B2 表格）。

**配音（B2）当前做不了，卡在网关没有 TTS 路由**（2026-09-22 实测，含对照组）：

| 实验 | 结果 | 说明 |
| --- | --- | --- |
| `POST /audio/speech` + `qwen-audio-3.0-tts-plus` | 400 `InvalidParameter: url error`，亚秒级，3/3 一致 | — |
| **`POST /audio/speech` + `qwen3.8-flash`（已知可用的对话模型）** | 400 同样报错 | **失败与模型无关 —— 是路径没实现** |
| `POST /images/generations` + `wan2.7-image` | 400 同样报错 | 图像路由也没有 |
| `POST /api/v1/services/aigc/text2audio/generate` | 404 | 该域名只提供 `/compatible-mode/v1` |
| 官方型号名 `qwen-tts` / `qwen-tts-latest` / `qwen3-tts-flash` / `cosyvoice-v1` | 404 | 这些 id 不在该套餐内 |
| **对照：`/chat/completions` + `qwen3.8-flash`** | **200 / 2 秒** | 网关本身是健康的 |

要接配音需要的是**一个真有 TTS 路由的上游**：阿里云百炼官方的
`dashscope.aliyuncs.com/compatible-mode/v1/audio/speech`（`qwen-tts` 系）确实支持，
只是这个 token-plan 代理没转发；或者换火山 / MiniMax 等带 TTS 的厂商。
**上游到位之前不要先摆音频节点** —— 用户已明确要求清掉过占位入口（宁缺勿假）。
应用侧的现状：`WorkflowNodeType` 里没有音频节点，`prisma` 的枚举里有 `AUDIO`，
素材库有音频标签页（只是浏览已存素材，不生成）。

**占位模型已从下拉里清掉**（2026-09-22）：`doubao-seedream-4-5-251128`（Seedream 4.5）
与 `nano-banana-pro`（Nano Banana Pro）这两个没有密钥的模型**已禁用**（不是删除 ——
标签与能力声明都留着，将来给这两个厂商配上密钥，在后台一键启用即可）。
禁用即从下拉消失：目录查询本身就是 `models: { where: { isEnabled: true } }`。
现在图片下拉只剩 `GPT Image 2` / `GPT Image 2.5`，默认仍是前者。

**同时堵住来源**：`scripts/seed-model-catalog.mjs` 原本把 4 个演示模型的 `isEnabled`
写成 `true`，谁重跑一次就又把占位符塞回下拉 —— 已改成默认禁用，并把原因写进脚本头。

**仍留着的同类占位**（用户只点名了图片那两个，这两个视频的没动）：`Seedance 1.0`
（volcengine-ark）与 `Kling v2`（kling）—— 同样没密钥，且视频本身就被 Key 分组挡着。

**图生图已实测可用**（这是电商出图的主路径，不依赖上面的解锁）：
`POST /v1/images/edits` 用 gpt-image-2 + 参考图 → 200 / 89 秒 / 返回 `b64_json`；
走应用自己的后端（`requestMode=image-edit`）→ 90 秒完成、落盘。
提示词「把背景换成纯白电商主图风格，保留产品本身不变」实测**主体保持不变**。

**`gpt-image-2.5` 已接入并实测**（2026-09-22）：
- 能力探测：三个档位 `1024x1024` / `1536x1024` / `1024x1536` 请求后**返回尺寸与请求完全一致**，
  即 size 是真被遵守的（另测 `999x999` 返回 `992x992`，说明它按某个网格取整，不是只认那三档）。
- 走应用后端端到端：`submit-generation.mjs` 指定 `MODEL_KEY=gpt-image-2.5` → 50 秒出图、
  落成文件、库里 `model_key=gpt-image-2.5`、产物是合法 PNG 1024×1024。
- 浏览器里模型下拉已出现「GPT Image 2.5」（顺序在 GPT Image 2 之后；**默认仍是 GPT Image 2**）。
- 计费 $0.04/张（比 2 贵 $0.01）。同一分组下 `-flare` / `-sunburst` 变体**未接入**（用户明确说只接 2.5）。

### B1. 视频生成策略 `后端`
- **依据**：`[公告]` Seedance 2.5 / MiniMax H3 Max / Wan 3.0；`[现状]` 服务端 `GenerationTaskStrategyKey` 只有 4 个值，**没有 video**
- **做法**：新增 `video` 策略（提交上游 → 轮询 → 落 `generation_outputs` → 结算 / 退款）；前端视频节点已经组装好参数，接上即可
- **涉及**：`server/generation-tasks/*`、`VideoNode.vue` 的 `handlePromptSend`
- **验收**：真出一条视频；失败路径（超时 / 上游报错）能正确退款

### B2. 音频生成节点 `上游阻塞`
- **依据**：`[实测]` `添加节点` 有「音频」；`[现状]` 无
- **做法**：新增 `audio` 节点类型 + 策略（TTS / 配乐两种模式）；产出可挂到时间线
- **验收**：能生成一段语音并播放
- **2026-09-22 状态：先不动。** 用户同意接配音，但实测两个上游都没有可用的 TTS 路由
  （判别实验与对照组见 3.0 的「配音（B2）」那段）。**缺的是能调用 TTS 的厂商，不是代码** ——
  上游到位前不摆音频节点。

### B3. 剧本生成 → **撤销卡片，功能并入画布 Agent**（2026-09-22 定案）

**"这两个功能现在解决了什么"——如实回答（这是砍掉它们的依据）：**

| 功能 | 实际交付 | 结论 |
| --- | --- | --- |
| LLM 文本节点 | 只交付了一件事：把上游文案**改写成生图提示词**（电商模板在用这一环）。它是文本链路唯一端到端验证过的实现（`submit-chat.mjs` 3 秒出文）。 | 有用，但这件事**画布 Agent 做得更好**——它有记忆（messages 10→12→14）、看得见画布、用的是同一个 qwen3.8-flash，还不占一张卡。 |
| 剧本节点 | **零交付**。09-21 注册，浏览器端到端从未验收（三次探针都卡在我自己的选择器上），行为与 LLM 节点完全相同，只多了句写死的"你是编剧"。 | 就是占位。 |

**撤销时的三个连带（下一轮一次做完）：**
1. `workflows.ts` 模板里的 llmConfig 卡片与连线一并移除（剧本/电商模板都要改）——
   文本生成的活归 Agent，卡片只留"产出素材"的：图片 / 视频 / 音频 / 素材。
2. 助手面板补**「把这段文字落到画布」**（一键生成文本节点）——现在 Agent 的产出
   只能复制粘贴，砍掉 LLM 节点后必须有这条出路，否则它的文本进不了流程。
3. 引用点清单（下次照着删）：
   `useWorkflowCanvas / node-suggestions / node-input-rules / upstream-inputs /
   reference-resolver / canvas-interaction(尺寸) / index.vue(组件映射) / CanvasMiniMap /
   canvas-brief / workflows.ts(模板)` + tests 五个文件

### 新增：音频节点（对齐 LibTV 的「音频」卡片）
- 卡片：文案输入 → 语音生成 → 播放/入库
- 类别走 prisma 已有的 `AUDIO` 枚举；模型先登记 qwen-maas 的 `qwen-audio-3.0-tts-plus`
- **上游真相（2026-09-22 晚，平台给了官方示例后破案）**：音频走的是 **DashScope SDK + WebSocket**
  （`wss://maas.qianwenaiapi.com/api-ws/v1/inference`），不是 REST —— 之前所有 REST 探测失败
  不是"没开通"，是**协议不同**。所以"列出了≠能调用"要再加一条：**也可能只是换个协议**。
  - 已有官方示例：`qwen-audio-3.0-asr-flash-streaming`（**识别**，Recognition 类，wav/16k）
  - **配音要的 TTS 示例还没有**：`qwen-audio-3.0-tts-plus` 在这套 SDK 里用哪个类、什么参数，待平台确认
- **接入方式**：后端是 Node，官方 SDK 是 Python/Java → 两条路：Node 直连 WS 协议，或 Python 小边车；等 TTS 示例到手再定
- **2026-09-22 深夜定案：音频先搁置** —— 用户去找别的音频接口；且现在的视频模型都带音轨输出，配音不急。
  音频节点等 TTS 调用方式确认后再做，**不先摆空节点**。

### B4. 智能剪辑节点 `后端`
- **依据**：`[实测]` 4 个场景 `讲解视频 / 批量广告 / 口播视频 / 素材混剪`，空态「请连接视频节点后操作」；`[公告]` 粗剪去口水词/停顿/重复、花字、画中画、音效、BGM、封面预览
- **做法**：需要视频转码 + 场景分析管线（最大的一块）
- **验收**：连 2 段视频 → 选「口播视频」→ 产出成片

### B5. 逐帧拉片节点 `后端`
- **依据**：`[实测]` 拆解维度 `分镜 / 动态 / 音乐`；`[公告]` v1.0 列了 4 个维度（叙事分镜/精彩运镜/动作片段/音频参考）
- **做法**：视频抽帧 + 多模态分析，输出可被下游复用的素材
- **验收**：拉片结果能接进下游节点

### B6. 视频加工系列 `后端`
- **依据**：`[公告]` v1.0：智能续写（任意时间点）、片段重拍（毫秒级时间戳）、深度动作捕捉
- **做法**：三个独立策略，可共用视频处理基础设施（与 B4 同批做更省）
- **验收**：各自能跑通一条

### B7. 图像加工系列 `后端`
- **依据**：`[公告]` v1.0：图层分离（人物/物体/文字/背景，移动缩放旋转层级景深）、宫格切分（可拖动分割线）、创意片头
- **做法**：图像分割 + 合成。图层分离与宫格切分都是"一张图变多张"，可共用管线
- **验收**：图层分离产出可独立编辑的图层

### B8. 智能引用 AutoLink `后端`
- **依据**：`[公告]`「输入提示词后，实时将相关参考素材智能 @ 引用到对应提示词后面」
- **做法**：需要素材语义检索 + 自动插入 token。**注意**：我们现在的自动注入是"整段拼接"，不是"插 token"
- **验收**：输入提示词后，提示词里出现位置正确的 @ 引用

---

## 4. 第三阶段：需要第三方 Key / 外部模块

### K1. 图片生成真正跑通 `Key` ✅
- **依据**：`[实测]` 我已把链路打到火山方舟并拿到 `AuthenticationError: API key missing or invalid`；`generation_records` 因此只有 1 条失败记录
- **做法**：后台配置供应商密钥（`ai_provider_configs` 目前空表）；顺带确认积分（我调试时记了 1000 分）
- **验收**：真出一张图并落进 `generation_records`（`status=SUCCESS`）
- **✅ 已完成**（2026-09-21）。配了 ggwk1 中转站（`https://www.ggwk1.online/v1`，
  OpenAI 兼容，11 个图像/视频模型），密钥走后台自己的接口写入（由后端加密）。
  新建模型 `GPT Image 2`（`gpt-image-2`），能力按**实测**填写：
  返回 base64（应用侧本来就支持，见 `upstream-shared-parser` 的 b64_json 分支）、
  单张 50~228 秒、n>1 在 5 分钟内没完成所以 `maxImagesPerRequest: 1`。

- **打通过程中发现并修掉一个影响面很大的 bug**（不是本次改动引入的，是一直存在）：
  `server/redis/config.ts` 的 `normalizeInteger()` 用 `Number(value)` 判非法，
  而调用方一律传 `process.env.X || ''` —— **`Number('')` 等于 0 而不是 NaN**，
  于是"未设置"被当成"显式配了 0"，最后 `Math.max(minValue, 0)` 返回 **1**。
  结果是**所有没在 .env 里配置的 Redis 参数全变成 1**，其中
  `taskLockTtlMs = 1 毫秒` 让任务执行锁瞬间过期、续期必然 `ownership_lost`
  → **任何耗时超过 5 秒的生成任务都会被中断**。
  这正是「这个库里从来没成功出过图」的直接原因：之前 seedream 的失败是上游秒回的
  认证错误（没活到 5 秒），真正开始生成的任务活不过 5 秒。
  修法：先判空字符串再判数值，并补了 15 项单测钉住（含"锁 TTL 不能是 1 毫秒"这条回归）。

- **上游超时也按实测放宽**：原来是 `90s + (n-1)×60s`（注释按直连官方 API 估的
  "单张 5-30s"）。实测这个中转站同一档参数耗时 50 / 82 / 195 / 228 秒，差 4 倍。
  基线改为 300s、每张 90s、上限 600s。它只是上限不是延迟。
- **实测证据**：数据库里 `COMPLETED` / 45 秒，产出 PNG 1024×1024 / 1.58MB；
  浏览器 K1 验收 5/5 通过（选节点 → 切模型 → 参数随模型变 → 出图 → 载荷正确）。
- 新增两个可复用脚本：`tests/e2e/real-generation.mjs`（浏览器端端到端验收）、
  `tests/e2e/submit-generation.mjs`（API 直接提交并轮询，用来隔离前后端问题）。

### K2. 3D 导演台 `后端` + 3D 模块
- **依据**：`[实测]` 「在3D空间中搭建场景并进行多视角截图」；`[公告]` v1.5 3D-BOX：Agent 搭场景、排角色动作与机位、多分镜预演、**手绘轨迹预演运镜**
- **现状**：`[现状]` 用户已有明确意向（"3D导演台都有开源现成的，接进来就好，它是单独模块"）
- **做法**：接开源 3D 场景编辑器，截图映射回画布作为参考图
- **验收**：在 3D 里摆好 → 截图 → 画布上出现参考图节点

### K3. 角色造型室 `后端`
- **依据**：`[公告]` v1.5：从人设/参考图/已有角色出发，可视化调五官发型服装、细化痣/雀斑/疤痕，关联音色、存角色卡跨项目复用
- **验收**：调完的造型能存成角色卡并在新项目复用

### K4. Agent 操作节点（TV Director）`后端`
- **依据**：`[实测]` 底部有 `TV Director`，展开后给 `感知画布开始创作 / 上传故事来改编 / 批量优化提示词 / 全能创作`；`[公告]` v1.5「【TV Director】支持新建、更新、运行画布节点」「支持 Agent 运行过程中切换自动/手动模式」
- **做法**：把画布状态作为工具暴露给 agent（已有 `agent-workspace` 策略可参考）
- **验收**：一句"帮我把这 5 个分镜批量加参考图"，agent 真的改了画布

---

## 5. 明确不做 / 存疑

| 项 | 结论 | 理由 |
|---|---|---|
| 站外链接的字节 CDN 素材 | **待清**（方案 A 已定：只修坏的） | 热链不算再分发，不是法律急事；但 `homeDiscoverContent.json` 41 处指向个人 OSS bucket，有失效风险 |
| 9 个「接入中」占位按钮（全景图 / HD 增强 / 编辑元素 / 角度 / 打光 / 裁剪） | **已接线**（除「编辑元素」） | 实测这些大多是"固定指令 + 图生图"，不需要新后端：裁剪纯前端，角度/打光/HD 增强/全景图 走已验证的 image-edit 管线。**编辑元素（局部重绘）做不了** —— 见下一行 |
| 「编辑元素」局部重绘（涂抹蒙版） | **待定**（不是"不支持"，见下） | 2026-09-21 实测记录，**先纠正一个误判**：第一次带 `mask` 的请求 13 秒返回 400，我据此判断"上游不收这个参数"——错了。**重试同一个带 mask 的请求返回 200、334 秒出图**；同图同提示词不带 mask 的对照是 200、265 秒。所以那句 400 是中转站的偶发失败（不带 mask 的请求也出现过同样一句），**不能作为"不支持蒙版"的证据**。目前只能确认「参数被接受」，**还没验证蒙版是否真的生效**（透明区外是否原样保留）—— 要验证只需再发一次带 mask 的请求、把 b64 存下来，对比圆外区域与输入图是否一致。**复用配方**（确认可用后照做）：`src/shared/upstream-request-normalizer.ts` 的 `buildImageEditRequestFormData` 增加 `mask` 入参并 `formData.append('mask', blob, 'mask.png')`（务必校验 `blob.type === 'image/png'`，jpg 没有 alpha）；`RequestImageEditInput` 与 `image-task-executor` 各透传一层；再配一个涂抹蒙版 UI。各层都很浅 |
| 无后端支撑的纯展示项 | **不做** | 宁缺勿假：摆一个点了没反应的入口，比不摆更差 |
| 重复的 `canvas.css` 导入 | 低优先 | `workflow.css` 与 `canvas-shell/index.css` 都引了 `canvas.css`，重复但无害 |

---

## 6. 执行顺序建议

```
第一批（纯前端，立即可做）   F1 折叠 → F2 参数分类型 → F5 预设分离 → F4 开关 → F3 chip 行
第二批（等 Key）             K1 图片生成跑通  ← 这一步能立刻看到真实产出
第三批（纯前端，独立）       F6 资产节点 → F7 工具条 → F9 性能压测
第四批（高风险）             F8 内联 chip（中文 IME）
第五批（后端）               B1 视频 → B3 剧本（最省）→ B2 音频 → B7 图像加工 → B4/B5/B6 视频加工
第六批（重）                 K2 3D 导演台 → K3 角色造型室 → K4 Agent 操作节点 → B8 AutoLink
```

**每一批的验收门槛统一**：
```bash
cd ~/CanvasMind
export PATH="$HOME/.local/lib/nodejs/bin:$PATH"
npx vue-tsc --noEmit 2>&1 | grep -E '^src/.*error TS' | grep -v 'art-design/ArtForm'   # 必须无输出
for f in tests/*.test.ts; do npx tsx "$f" | tail -1; done                              # 全部 失败 0
# 浏览器（需要一个有效 session）
for s in route-sweep drag-to-create double-click-create mention-reference; do
  SESSION_TOKEN=<token> node tests/e2e/$s.mjs | tail -1                             # 12/18/27/27 不许退化
done
```

---

## 附录 A：LibTV 实测原文（逐字，2026-09-21 登录后抓取）

留着这段的原因：会话会过期，以后要用不必重新登录再抓一遍。

### A1. 添加节点面板（底部工具条 `+`）

```
添加节点                              [搜索]
────────────────────────────────────────────
文本
图片
视频
智能剪辑      Beta
导演台        NEW
逐帧拉片      ⬥ IM SD 2.5              ← 带模型绑定标记
音频
脚本                                  ▸ 脚本 NEW / 脚本（旧版）Beta
素材库                                ▸ 风格库 / 特效库
────────────────────────────────────────────
添加资源
上传
从生成历史选择
```

内部 `react-flow__node-*` 类型值：`image` / `video` / `shot`(逐帧拉片) / `director-console-3d`(导演台) / `director`

### A2. 各类节点的卡片正文（逐字）

**图片节点**
```
图片节点 1
尝试：
  图生图
  图片高清
[+参考] [标记] [风格]                    ⤢
placeholder: 可直接文字生图，或上传图片输入文字指令对图片进行编辑，如：将背景改为雪夜
composer: [Lib Image 2.5 Pro] [16:9 · 标准画质 · 2K · 1张] 15 [高级设置] [智能引用 AutoLink]
DOM 锚点: gen.prompt / gen.model / gen.params.count / gen.submit
```

**视频节点**
```
视频节点 2
尝试：
  5分钟超长视频
  首尾帧生成视频
  首帧生成视频
[参考] [标记] [特效] [角色库] [运镜]
placeholder: 描述你想要生成的画面内容，@引用素材
composer: [2.0] [文生视频] [16:9 · 720P · 5s · 1个]
          [高级设置] [联网搜索] [自动校验素材] [智能引用 AutoLink]
```

**智能剪辑**（`type=video`，350×350）
```
智能剪辑 1
空空如也，请连接视频节点后操作
尝试：
  讲解视频
  批量广告
  口播视频
  素材混剪
[参考]  placeholder: 描述想剪成什么效果
composer: [默认模式] [16:9 · 720P · 30s]
```

**逐帧拉片**（`type=shot`）
```
逐帧拉片 | SD 2.5
[视频素材] 上传视频后开始
拆解维度： 分镜 / 动态 / 音乐
[开始拉片]
```

**导演台**（`type=director`）
```
导演台 3
在3D空间中搭建场景并进行多视角截图
[打开导演台]
```

### A3. @ 素材引用（在提示词里敲 `@`）

```
素材引用
  图片        data-reference-kind="image"
  视频        data-reference-kind="video"
  音频        data-reference-kind="audio"
  文本        data-reference-kind="text"
```
无可引用资产时第二级显示：`暂无可引用资产，请连入后操作`
（DOM 标记：`data-script-mention-dropdown` / `data-reference-main-panel`，面板宽 200px，定位在输入框上方）

### A4. 画布外围 chrome（按钮 title / 文案）

```
顶部： [工作区名 ▾] [画布 1 ▾]  [布局切换]   分享 · 会员 · ⚡20积分 · 通知 · 头像
底部中： 添加节点 | 移动 | 素材库 | 角色造型室 | 生成历史 | 快捷键 | 教程 | ● TV Director
底部左： 资产管理 | 整理画布(Option+Shift+F) | 切换小地图 | 隐藏节点连线 | 网格吸附 | 100%
节点：   标题在卡片上方（带 ▶ 折叠）；左右 ⊕ 手柄在卡片垂直中线；composer 停靠在卡片下方
画布：   点阵网格，间距 16px，圆点 r=0.5，色 #474747，底 #141414
TV Director 展开： 感知画布开始创作 / 上传故事来改编 / 批量优化提示词 / 全能创作
右键节点： 复制 / 删除（带快捷键提示）
```

### A5. 官方更新记录摘录（`liblib.tv/feature-updates`）

**v1.5（2026-09-20）五大独家**
- 剧本原创与改编：按题材/人设/背景辅助原创；台词冲突局部改写**对比确认**；关联修改提示对大纲/钩子/伏笔的影响
- 导演执导：选预设导演或导入自己的方法与提示词模板，生成「导演分身」；辅助拆镜、**创建节点**、批量优化提示词；手动模式先审核计划再生成
- 角色造型室：从人设/参考图/已有角色出发，可视化调五官发型服装、细化痣/雀斑/疤痕；关联音色、存角色卡跨项目复用
- 创意片头：基于选中画面设计片名、生成文字动效、字幕识别与样式；口播视频支持自动粗剪、画中画、B-Roll 匹配、音效配乐
- LibTV 3D-BOX：文字/参考图 → Agent 搭场景、排角色动作与机位、多分镜预演；**手绘轨迹预演运镜**

**v1.5 其他优化**
- 【TV Director】支持新建、更新、运行画布节点
- 【TV Director】Agent 运行过程中切换自动/手动模式
- 【画布优化】打开速度、数千节点卡顿、偶尔丢失节点

**v1.0**
- 深度动作捕捉（提取深度信息，动作/运镜参考不受原风格干扰）
- 宫格切分（hover 分割线变左右箭头即可拖动）
- 图像图层分离（人物/物体/文字/背景，移动/缩放/旋转/层级/景深）
- 智能剪辑优化（素材粗剪去口水词/停顿/重复；动态花字、人物画中画、音效、BGM；封面预览）
- 视频智能续写（任意时间点起，可加参考图/视频）
- Blender 插件（录制 3D 白模视频/截图 → 一键发送到 LibTV）
- Seedance 2.5 超 5min 视频直出
- 逐帧拉片（叙事分镜 / 精彩运镜 / 动作片段 / 音频参考）
- **智能引用 AutoLink**（输入提示词后实时把相关参考素材智能 @ 引用到对应提示词后面）
- 视频片段重拍（毫秒级时间戳定位，选一段或多段重拍，文字描述改动）
- 智能剪辑（口播视频 / 批量广告 / 智能混剪 / 无人出镜解说视频）


## 画布交互重构（2026-09-22 定稿，下一轮第一优先）

用户原话的四个决定，逐条钉死：

1. **卡片不要有任何内容。**
   - 图片卡 = 纯图（生成中→转圈；失败→一行原因）。标题栏「文生图」、参数行
     （模型/比例/画质/张数/高级设置/自动校验素材）、选中工具栏（8 个按钮）、
     替换/裁剪/下载按钮 → **全部移除**。
   - 预览保留双击；节点删除必须保留入口（Delete 键 / 右键菜单）—— 删控件前先确认这个。
2. **输入框维持现状，不改。**（用户确认输入框没问题；
   之前提的「选中即跟随」改版**作废**，不再做。）
3. **卡片尺寸按类型区分**：文本卡最小 → 图片卡按自身比例 → 视频卡最宽（16:9）。
   现状：尺寸约束在 `canvas-interaction.ts`（NODE_SIZE_DEFAULTS），
   但卡片实际大小被 min-width 和内容撑开 —— **瘦身是尺寸生效的前提**，所以先做 1。
4. 画布 Agent 保留（全局入口），但卡片上**不再重复**它的能力。

**为什么这样是对的**（用户原话：「明明一个输入框可以解决所有文图，能文生图，图生图」）：
卡片上的控件堆叠，唯一的成因是输入框不跟卡片走、每张卡被迫自给自足。
现在输入框被确认是好的，那卡片的控件就全部失去存在理由 —— 拿掉即可，不用造新交互。

**执行顺序**：瘦身（1）→ 尺寸（3）→ 回归（拖拽/连线/删除/双击预览 全部重测）。
文本卡保留文本本身（那就是它的产出），视频卡用首帧画面。
**验收**：改完的用户视角 —— 图片卡上除图以外一个字都没有；三种卡一眼能从大小分辨类型。


## LibTV 画布实测复核（2026-09-22，登录后逐项抓取）→ 改造队列 G1–G10

> 这一节是**实测复核**，不是公告转述。所有尺寸都是登录后在真实画布上用
> `getBoundingClientRect` / `offsetWidth` 量出来的，缩放值同时记录，
> 表里给的都是**未缩放布局尺寸**。
>
> **证据截图**（都在 `~/artifacts/`，命名 `libtv-canvas-NN-*.png`）：
> `03` 添加节点菜单 · `04` 脚本子菜单 · `05` 素材库子菜单 · `06` 文本卡 ·
> `11` 八类节点同屏 · `13` 图片参数面板 · `14` 视频输入框 · `15` 视频参数面板 ·
> `17` 高级设置展开 · `19` 连线剪刀 · `21` @ 引用面板

### 实测数据（照抄，别凭印象改）

**节点卡片**

| 类型 | DOM 类型 | 布局尺寸 | 形状 |
|---|---|---|---|
| 文本 | `text` | 350 × 350 | 正方 |
| 音频 | `audio` | 350 × 350；**有音频文件时 350 × 148** | 正方 → 扁条 |
| 智能剪辑 | `video-clip` | 350 × 350 | 正方 |
| 导演台 | `director-console-*` | 350 × 350 | 正方 |
| 逐帧拉片 | `shot-breakdown` | 320 × 389 | 竖长 |
| 图片 | `image` | 622 × 350 | 16:9 |
| 视频 | `video` | 622 × 350 | 16:9 |

圆角 12px、1px 描边。**卡片高度不是固定的**（音频加载后高度收缩）。

> **⚠️ 纠正一处我自己的误判**：上表里图片/视频那行写「622 × 350」是**当时那一个节点的状态**，
> 不是固定值。生成类节点的尺寸**跟「比例」参数走**。判别实验：新建一个空视频节点，
> 把比例从 16:9 换成 9:16，节点从 `622×350` 变成 `350×622`。
> 所以规则是「长边 622 / 短边 350，朝向由 ratio 决定」，见 `src/views/workflow/config/node-size.ts`。
标题在卡片外上方，13–14px；**视频节点标题右侧带 `1280 × 720`**，图片节点只有名字。

**输入框（composer）**

| 节点 | 宽 | 高 |
|---|---|---|
| 文本 | 660 | 150 |
| 图片 | 660 | 192 |
| 视频 | 660 | 255 |
| 视频（展开高级设置） | 660 | 255 + 157 |

**输入框不随画布缩放**（画布 40% 与 75% 时都是 660 宽，而卡片在缩）。
提示词是 `contenteditable`，`aria-placeholder="描述你想要生成的画面内容，@引用素材"`，min-height 80px。

**参数面板**：都是 342 宽（图片 321 高 / 视频 448 高）
- 图片：画质（标准/高画质）· 清晰度（1K/2K/4K）· 比例（当时模型只给 16:9）· 数量（1/2/4 张）
- 视频：比例（**Auto/16:9/4:3/1:1/3:4/9:16/21:9**，带比例图形）· 清晰度（480P/720P/1080P）·
  **视频时长（滑杆 + 单位 s）** · **生成音频（开启/关闭，带 ⓘ）** · 数量（1/2/4 个）

**高级设置**：`grid-template-rows: 0fr → 1fr` 300ms 折叠区，触发键是输入框底部**链接图标**；
视频 3 开关（联网搜索 / 自动校验素材 / 智能引用 AutoLink），图片 1 开关（AutoLink）。
开关 38×20，每行 626×36。

**空态不是空的**（这是本轮最该抄的一条）：
- 文本 = 文档图标 + 「尝试：自己编写内容 / 文生视频 / **图片反推提示词** / 文字生音乐」
- 音频 = 只有 1 项「音频生视频」
- 智能剪辑 = 输入声明「**空空如也，请连接视频节点后操作**」+ 4 项（讲解视频/批量广告/口播视频/素材混剪）
- 导演台 = 功能说明 + 按钮「打开导演台」
- 逐帧拉片 = 分区块表单：**「视频素材」dropzone → 「拆解维度」分镜/动态/音乐 → 底部「开始拉片」（条件不满足时禁用）**

**节点级工具栏**（选中时锚在卡片上方居中）：图片 9 项（人像质感调节 NEW / 全景 / 多角度 / 打光 /
九宫格 / 高清 / 元素编辑 / 图层分离 / 宫格切分，带 ▾ 的有子菜单）；视频 7 项（高清 / 片段重拍 /
逐帧拉片 / 智能去字幕 / 音视频分离 / 主体消除 / 创意片头）。
**已知缺陷**：工具栏跟着节点水平居中，节点贴左边时整条被裁到屏幕外（实测触发）。

**连线与引用**：悬停连线 → 19×19 剪刀按钮（剪断）。联动是**节点级**的：连上的图片/音频以
**缩略图 + 编号**出现在输入框里（音频标 `1`、`7.7s`），不是纯文本 token。
`@` 面板 200 × 224：搜索框 → 「素材引用」→ 图片 / 视频 / 音频 / 文本（各自二级）。

---

### G1. 卡片空态：给每类节点一个**真实可用**的最小行动项 `前端` —— **图片节点已完成**

> **图片节点已按实测落地**（2026-09-22）：空态 = 「尝试：图生图 / 图片高清」，两项都是真行为 ——
> 图生图拉起本地上传（落到本节点），图片高清走已验证的 image-edit 管线（结果落新节点）。
> 输入框 placeholder 也换成了 LibTV 的原文：「可直接文字生图，或上传图片输入文字指令对图片进行编辑，如：将背景改为雪夜」。
> **实测确认**：空图片卡上点「图生图」会真的拉起文件选择器。
> **待补**：视频节点的「尝试」列表（现状已有生成方式列表，需与 LibTV 三项对齐核验）、
> 以及将来上新类型时照抄这个句式。

- **证据**：上面「空态不是空的」那一段。
- **现状**：上一轮瘦身后，空图片卡是一块纯空块（用户原话「卡片呢，大佬，你只留一个输入框有什么用啊」）。
- **做法**：图片卡空态给两项**已有真实行为**的动作 —— 「上传图片」（`ImageNode` 里原有的上传链路）
  与「文生图」（滚到/唤起下方 composer）；视频卡沿用它的 feature 列表。
  **不许摆没有实现的项**（LibTV 那 4 项点下去都有行为；我们的「接入中」已经吃过一次教训）。
- **验收**：空图片卡不再是纯空块；卡上每一项点下去都有真实结果；`vue-tsc` + 单测全绿。

### G2. 尺寸对齐到实测规则 `前端` —— **已完成（图片/视频/文本）**

- **证据**：工具类固定 350×350（逐帧拉片 320×389）；生成类**跟比例走**（长边 622 / 短边 350）。
- **做法**：新增 `src/views/workflow/config/node-size.ts` 作为**唯一来源**：
  `resolveGenerationCardSize(ratio)` 负责生成类，`CANVAS_TOOL_NODE_SIZE` 负责工具类；
  三个节点组件只绑 `cardSizeStyle(...)`，不再各写一份 min-width。
  图片/视频的内容一律 `object-fit: cover`（LibTV 同款：非当前比例的图被裁切，而不是让卡片变形）。
- **实测确认**（`npm run type-check` + 16 个单测 + 浏览器）：
  文本 350×350、视频 622×350、图片 350×350 —— 图片是正方形**因为它当前的比例参数就是 1:1**，
  规则本身与 LibTV 一致。
- **默认档已改成横版（2026-09-22，用户拍板「跟 libtv 一致」）**：图片默认从 `1024x1024`（1:1）
  改为 `1536x1024`（3:2 横版），空图片卡因此从 350×350 变成 **622×350**，与 LibTV 一致。
  **实测确认**：改后 `offsetWidth` = 622×350（文本节点仍 350×350，同屏一眼可辨）。
  - 只用了模型真支持的横版档，**没有编造 16:9**（gpt-image 系列不支持 16:9）。
  - ⚠️ **这个默认值存在数据库里、不在仓库里**：`ai_models.default_params_json.size`。
    两行（`gpt-image-2` / `gpt-image-2.5`）原值都是 `1024x1024`，已改为 `1536x1024`。
    回退：`UPDATE ai_models SET default_params_json = JSON_SET(default_params_json, '$.size', '1024x1024') WHERE model_key IN ('gpt-image-2','gpt-image-2.5');`
  - 目录接口有 **60 秒 Redis 缓存**（`DEFAULT_CACHE_TTL_SECONDS`），改完要等一分钟或重启服务才生效 ——
    这次踩到了：库里已经改了，接口还发旧值。
  - 代码侧同步：`OPENAI_IMAGE_SIZES` 改成横版打头，和 Seedream / Nano Banana 两张表的既有约定一致
    （那两张表本来就是横版打头），这样即使目录里没声明默认档，新建节点也是横版。
- **单测**：`tests/node-size.test.ts`（28 项）锁死「跟比例走」「认不出回落到横版」「不会返回 0 尺寸」。
- **未覆盖**：`src/types/canvas-interaction.ts` 里那份 `NODE_SIZE_DEFAULTS` 现在**没有任何消费者**
  （尺寸改由 node-size.ts 提供），属于死代码，留到下一次清理（避免与本次改动混在一个提交里）。

### G3. 输入框：固定宽度 + 不随画布缩放 + 按内容增高 `前端` —— **已完成（宽度与缩放）**

- **证据**：660 宽固定；高 150/192/255；不随缩放；高级设置展开 +157。
- **现状**：`ContentGenerator` 跟随容器宽度，节点被画布一起缩放 → 缩小画布时输入框跟着糊。
- **做法**：输入框固定 660 宽并脱离画布缩放（node 内做反向缩放，或挂到画布外的浮层）。
- **做法（已落地）**：新增 `src/views/workflow/composables/useComposerPanel.ts` 作为唯一实现 ——
  按当前 zoom 取 `scale(1/zoom)` 精确抵消 viewport 的 `scale(zoom)`，锚点 `top center`
  （输入框挂在卡片正下方，锚点跑偏会相对卡片左右乱飘）。
  **图片 / 视频 / 文本三个节点共用这一个 composable**，各自的 `min-width/max-width/transform` 全部删掉。
- **实测确认（浏览器，`getBoundingClientRect`）**：
  | 节点 | 80% 缩放 | 200% 缩放 | 节点自身（屏幕宽） |
  |---|---|---|---|
  | 图片 | 660 | 660 | 280 → 700 |
  | 视频 | 660 | 660 | — → 1244 |
  即输入框屏幕宽度恒定 660，而卡片在正常缩放 —— 与 LibTV 一致。
- **未覆盖**：文本节点的输入框**没有单独实测**（它和上面两个共用同一 composable 与同一种绑定；
  当时文本节点在视口外、且本地未登录的浏览器反复弹登录框抢走点击）。
  在本地登录状态下补测一次即可闭环。
- **输入框与卡片的关系**（逐项对照，2026-09-22 核对）：

  | 关系 | LibTV 实测 | 我们（本轮改后） |
  |---|---|---|
  | 挂载位置 | 卡片正下方 | 同 |
  | 水平对齐 | 与卡片水平居中 | 同（`translateX(-50%)`，反向缩放后中心仍落在卡片中心） |
  | 与卡片间距 | ≈12px | **12px**（图片节点原写 18px，本轮统一） |
  | 宽度 | 固定 660，三类节点一致 | 660 |
  | 随画布缩放 | 不缩 | 不缩（实测 80%/200% 恒 660） |
  | 出现/隐藏 | 选中出现、取消选中消失 | 同（`isSelected && !loading`） |
  | 超出卡片边界 | 允许（660 > 622） | 同 |
  | 高度 | 按类型与内容（150/192/255，展开高级设置 +157） | 按内容自适应（**未**强写类型高度） |

- **遗留**：高度没有按类型写死 —— 这一条是「按内容自适应」，我们的 ContentGenerator 本来就自适应，
  不强行对齐数字；真要对齐得先有「文本输入框为什么要 150 而不是 120」的实际收益，否则只是抄数字。
- **遗留**：输入框内部的 **chip 行**没有逐项核对（LibTV 图片是「参考 / 标记 / 风格」三个、
  视频是「参考 / 标记 / 特效 / 角色库 / 运镜」五个；我们目前只见到「参考 / 风格」）。
  这一条属于 G4 参数集范围，等做到 G4 时一起核。
- **风险记录**：反向缩放会踩到 `nodrag/nowheel`、`screenToFlowCoordinate`、弹层定位；
  本次只改了浮层自身，没有动坐标换算，所以风险面比预想小。

### G4. 参数集按类型补齐 `前端` + `后端/Key` —— **已完成能做的部分（时长滑杆）**

- **证据**：视频是 5 个维度（比例/清晰度/时长/生成音频/数量），图片是 4 个。
- **现状**：参数按模型能力走；视频**缺「数量」与「生成音频」**；时长是下拉不是滑杆。
- **先核对，再动手（这一步很关键）**：上表里我写的「视频缺数量」是**错的**。
  实测我们的视频输入框早就有 4 个维度，摘要芯片就是 `16:9 · 720P · 5s · 1个`（与 LibTV 同格式）：

  | 维度 | LibTV | 我们（核对后） | 结论 |
  |---|---|---|---|
  | 比例 | 7 档（含 Auto），图形单选卡 | 6 档文本 chip | 档位差异来自模型声明；图形卡归 G5 |
  | 清晰度/分辨率 | 480P/720P/1080P | 720P/1080P | 同上，档位由模型声明 |
  | 视频时长 | **滑杆 + 单位 s** | 原先也是 chip → **本轮改成滑杆** | ✅ 已对齐 |
  | 生成数量 | 1/2/4 个 | 已实现，但当前隐藏 | 见下 |
  | 生成音频 | 开启/关闭（带 ⓘ） | **没有** | ⛔ 上游阻塞 |

- **视频时长改滑杆（已完成）**：档位仍只取**模型声明的离散值**，所以滑杆走「下标」而不是秒数 ——
  直接绑秒数会在模型只认 5/10 时给出 7 秒这种上游不认的值。
  **实测联动**：在滑杆上按方向键 → 值显示 `10s`、摘要芯片同步变成 `16:9 · 720P · 10s · 1个`。
- **「生成数量」为什么看不到**：控件早就实现了，但两个视频模型在目录里都**没有声明
  `maxImagesPerRequest`**（实测 `capabilityJson` 里没有这个字段），schema 回落成 1，
  而按设计「上限 ≤ 1 时不显示」（显示一个按不动的控件是噪音）。
  → 若确认上游支持多条，在后台把上限填上即可出现，**这是配置不是代码**；
  我不能替上游声明它没验证过的能力。
- **「生成音频」为什么不上**：`server/` 里没有任何音频开关字段（`generate_audio` / `withAudio` 全无），
  docs 里也已判定音频上游到位不了（TTS 走 `/audio/speech` 返回 400，是路径没实现）。
  按「宁缺勿假」与用户「没有的后面补上」的口径，**跳过并记录**，不摆开关。
- **输入框 chip 行（记录差异，本轮不动）**：我们目前是「参考 / 风格」两个（都有真实行为）；
  LibTV 图片是「参考 / 标记 / 风格」三个、视频是「参考 / 标记 / 特效 / 角色库 / 运镜」五个。
  缺的这几项**各自需要一套素材库或语义**（特效库、角色库、标记的时间轴语义），
  我们没有对应数据源 —— 不摆空 chip。等这些库到位再补。

### G5. 比例选择器做成图形单选卡 `前端` —— **已完成**

- **做法（已落地）**：新增 `src/components/generate/RatioChoiceGrid.vue`，图片与视频节点共用。
  图形按真实比例绘制，**同时适配 30×22 的上限**（只夹宽会在 1:1 上算成 30×30、顶破图形区 —— 实测复现过，
  同一排的卡片高度参差不齐）；边框走 `box-sizing: border-box`，否则每格会大 2px、比例也偏。
  `auto` 之类认不出比例的档位画虚线方框（表达「不指定」）。
- **顺带分层修正**：比例解析从 `views/workflow/config/node-size.ts` 提到 `@/config/model-params`
  （图形卡与节点尺寸都要用），旧的导出名留了别名，测试与调用方无需改动。
- **实测确认**：网格 260×114、**4 列**、每格高 54px（换行后高度一致）；
  图形尺寸 16:9 → 30×17、21:9 → 30×13、4:3 → 29×22、1:1 → 22×22、3:4 → 17×22、9:16 → 12×22。

### G6. 节点级工具栏：只恢复**已接线**的能力 `前端` —— **图片节点已完成**

- **证据**：LibTV 图片 9 项 / 视频 7 项，全部真接线。
- **现状**：上一轮被我整条砍掉（原因是当时里面混着假入口）。
- **做法（已落地）**：只恢复已验证有产物的 5 项，**「编辑元素（局部重绘）」不上**（蒙版是否真生效仍未验证）：
  | 条目 | 后端行为 | 验证方式 |
  |---|---|---|
  | 高清 | `ENHANCE_PRESET` → image-edit，结果落新节点 | 未点击（会消耗积分），走的是已验证过的同一条管线 |
  | 全景 | `PANORAMA_PRESET` + 横版尺寸意图 | 同上 |
  | 多角度 | 下拉 6 项（正视/侧视/45° 侧脸/俯视/仰视/背面，各带说明） | **实测点了**：下拉真的展开出 6 项 |
  | 打光 | 下拉 6 项（影棚柔光/伦勃朗光/黄昏暖光/逆光轮廓/顶光/冷调夜景） | DOM 已确认 6 项 |
  | 裁剪 | 本地裁剪 → 上传 → 落新节点 | **实测点了**：打开真对话框（自由/1:1/4:3/3:4/16:9/9:16 + 「裁剪并生成新节点」） |
- **新增一条实测规则**：工具栏**只在节点有产出时出现** —— 空图片节点被选中时 LibTV 不显示工具栏
  （实测：选中空节点，工具栏元素数为 0），那时卡片里给的是「尝试」列表。我们照抄。
- **定位（实测对齐）**：浮在卡片上方居中、距卡片顶 **29px**、按钮高 **32px**；
  和输入框一样**不随画布缩放**（40% 与 75% 两档下 LibTV 每个按钮的像素宽高完全一致）。
  实现见 `src/views/workflow/composables/useNodeToolbar.ts`。
- **顺带修掉的缺陷**：LibTV 按卡片中心死对齐，节点贴边时整条被裁（实测第一项跑到 x=-355）。
  我们加了屏幕空间的水平收敛。
- **实测确认（我们的）**：820 宽窗口、卡片中心 749、工具栏 396 宽 → 收敛到中心 610
  （正好是「右边界 820 - 12 边距 - 198 半宽」），完整落在画布内；间距 29px；
  80% 与 200% 两档缩放下工具栏恒为 **396×42**（卡片本身从 448×252 变到 1244×700）。
- **单测**：`tests/node-toolbar.test.ts`（10 项）—— 含浏览器实测的那组数字，
  以及「任意位置 × 任意宽度下都完整可见」的扫描。

### G7. 引用从「纯文本 token」升级为「缩略图 chip」 `前端` —— **第一步已收口（含一处自查纠正）**

- **证据**：视频输入框里引用是 48×48 缩略图 + 编号（音频还标时长 `7.7s`）。
- **现状**：我们是 `@图片1` 纯文本 token。
- **自查纠正**：我原先写「第一步还没做」是**错的** —— 输入框里早就有「已引用」行，
  缩略图 + 类型角标 + 删除按钮都在（`mentioned-references`）。本轮做的是**对齐与补漏**，不是从零建。
- **本轮做的三件事**：
  1. **尺寸与角标对齐实测**：缩略图 40 → **48×48**（LibTV 实测值），角标从类型名（「图片」）
     换成 **`@图片1`**（直接告诉用户该写哪个 token，等价于 LibTV 缩略图上的编号）；
     顺带补 `box-sizing: border-box` —— 默认 content-box 下 1px 描边会把 48 渲染成 50（实测确认修好）。
  2. **补上 G8 留下的缺口**：自动引用的素材原先**在界面上看不见**（只有开关旁一句计数）。
     现在它们也进「已引用」行，用虚线描边 + 「自动」角标区分，且**不给叉号**（没有 token 可删）。
     实测又踩到一个：那一行的 `v-if` 只看用户 token，导致「开了 AutoLink 但没手写 @」时整行不渲染 ——
     判据改成 `referencedMediaItems.length || autoLinkedMediaItems.length` 才对。
  3. **实测确认**：连一条 image → image 的上游后，下游节点的「已引用」行出现 48×48 虚线缩略图、
     角标「自动」、tooltip「自动引用：图片1」、无叉号；开关旁显示「自动引用 1 个」。
- **第二步已完成（2026-09-22），但做的是另一件事 —— 先纠正我的误判**：
  我原先说第二步是「把 @ 引用画进输入框内」，那是我对自己实测数据的过度解读。
  回到实测：LibTV 的引用就是**缩略图行 + 每个缩略图右下角一个 `@` 按钮**（12×12），
  正文里是普通文本，没有内联 chip。所以第二步的正解是**给缩略图加 `@` 快捷插入**，
  而不是改编辑器 —— 风险因此从「中文 IME」级别降到普通插入逻辑（插入管线本来就有）。

  **做法**：缩略图右下角加 `@` 按钮，点了把该素材的 token 插入提示词。两处取舍：
  1. **按光标插入**，拿不到光标（输入框没聚焦 / 选区在别处）就追加到末尾 ——
     用户在段中间点缩略图时，token 不该跑到段尾；为此给 `InlineMentionInput` 暴露了 `getCaretOffset()`；
  2. **自动引用的缩略图也给这个按钮**：点一下等于「把它钉进提示词」，该素材从自动引用变显式引用。
  走的仍是敲 `@` 选素材的**同一条** `insertReferenceToken` 管线，没有第二套插入逻辑。
  按钮做成 16×16 而不是 LibTV 的 12×12：那个尺寸是装饰，这个是可点按钮，得留够命中面积。

  **实测确认（2026-09-22）**：连一条 image → image 后，在下游点缩略图上的 `@` ——
  正文里真的出现内联 chip（`<span class="inline-mention-chip" data-mention-token="@图片1">`），
  同时那个缩略图从「虚线 + 自动角标 + 无叉号」变成「实线 + `@图片1` 角标 + 有叉号」，
  开关旁「自动引用 1 个」随之消失 —— 自动引用 → 显式引用这条转换是通的。

### G8. 「智能引用 AutoLink」做成真开关 `前端` —— **已完成（端到端留待登录后跑）**

- **证据**：视频/图片的高级设置里都有这一项，默认状态可见。
- **现状**：我们的行为相当于常开、但**不往提示词里写 token**（清单 §1 记过这条）。
- **做法（已落地）**：开关落在输入框底部「高级设置」里（与「自动校验素材」并排），
  行为和 LibTV 的文档描述一致：**上游连入、而你没 @ 的媒体素材自动进这次提交** ——
  正文里补 `@图片1`（经 `resolvePromptReferences` 变成 `【图片1】`），URL 照旧走请求体。
  关掉后只用显式 @ 的；**节点侧原本「没有 @ 就注入全部上游」的兜底也一起停** ——
  这条用 send options 里的 `autoLink` 传下去，否则关了开关上游图还是会被塞进来（关了个寂寞）。
- **三条排除规则**（写在 `src/components/generate/auto-link.ts`，带单测）：
  开关关掉 → 一个不带；**文本类素材永不自动带**（它的语义是「把正文塞进提示词」，自动塞会改写意图）；
  已被显式 @ 过的不重复带。
- **开关状态**：与「自动校验素材」一样存全局偏好（localStorage `canana:generator:auto-link`，默认开）——
  这是使用习惯，不是某张卡的参数。
- **UI 如实告知**：开着且有候选时，开关旁显示「自动引用 N 个」，不让用户在看不见的情况下被塞素材。
- **单测**：`tests/auto-link.test.ts` 13 项（开/关、已引用去重、文本类排除、空资产原样返回、空输入不产生孤立 token）。
- **浏览器实测**：开关出现在输入框底部「高级设置」里（与「自动校验素材」并排），默认开
  （`aria-checked=true`）；点一下 → `false` + tooltip 变成「已关闭：只提交你在提示词里显式 @ 的素材」
  + `localStorage['canana:generator:auto-link']` 从 `1` 变 `0`，再点回来恢复 `1`。
  没有上游连入时「自动引用 N 个」提示**不显示**（计数为 0 时不占位）。
- **端到端实测（2026-09-22，登录后真跑）**：三条真实提交记录对照 ——

  | 开关 | 提示词 | 请求体里的参考图 |
  |---|---|---|
  | 开 | `把背景换成雪夜 【图片1】` | 上游那张 |
  | 关（**修复前**） | `把背景换成晴天`（无 token） | **上游那张 —— bug** |
  | 关（修复后） | `换成正午阳光`（无 token） | **`[]`** |

- **这一跑抓出一个真 bug（单测抓不到的那种）**：composer 把上游参考图**并进了同一个数组**
  (`imageReferenceImages`)，合并之后就分不出「用户自己传的」和「上游自动带的」——
  于是关掉开关后提示词里确实没有 token 了，但**请求体里照样带着上游图**，
  等于开关只关了一半。修法：记住「已合并进来的上游那部分」(`externalMergedUrls`)，
  关掉开关时按它把上游那份同时从 UI 与请求体里摘掉，用户自己传的一张不动；
  视频的首帧/尾帧映射同样受开关控制。
  纯逻辑抽到 `auto-link.ts` 的 `mergeReferenceImages`，单测补了 9 项（含这个 bug 的回归）。
- **其余实测确认**：开关 ON 时参考图行 1 张 + 「自动引用 1 个」；切到 OFF 后参考图行归零。
- **AI「生成」角标（正向也补上了）**：真跑一次图生图，节点出图后角标出现（`AI生成`）✓。

### G1 收尾（视频节点「尝试」列表）：**核对后决定不改，理由记录在案**

- **LibTV** 的视频空卡是「尝试：5分钟超长视频 / 首尾帧生成视频 / 首帧生成视频」—— 三项都是**任务预设**。
- **我们**是「连接图片节点作为首帧，或直接描述画面生成」+「生成方式：文生视频 / 图生视频 / 首尾帧」+「上传视频」，
  其中那三项**写的是 `feature` 参数**（互斥的模式选择，点了会改节点 data 并同步给工具栏）。
- **所以不把标签改成「尝试：」**：那会把一个参数选择器说成一组并列快捷键，名实不符。
  我们缺的那三项预设（如「5分钟超长视频」）**受限于模型能力**——seedance 只声明了 5/10 秒，
  没有 5 分钟档，摆上去就是假入口。`inputState.emptyLabel`（「连接图片节点作为首帧…」）
  已经是「节点自己说清要什么」的正确写法，保留。

### 顺手清债：`NODE_SIZE_DEFAULTS` 已删除

尺寸自 2026-09-22 起由 `views/workflow/config/node-size.ts` 统一提供，`src/types/canvas-interaction.ts` 里
那份按类型写死 min 宽高的表**已经没有任何消费者**（grep 确认只有定义处）。留着只会让人以为改它有用，已删，
并在原处留了一行注释说明去向。`WorkflowNodeType` 仍在别处使用，保留。

### 用户实测报回的两处输入框布局问题（2026-09-22）—— **已修**

用户原话：「卡片输入框有这个选项被遮挡了，而且这个自动校验素材这个文字排版也不行啊」。
两处都量到了具体数字，不是观感问题：

| 现象 | 实测 | 根因 | 修法 |
|---|---|---|---|
| 有选项被「遮挡」 | 「智能引用 AutoLink」整块在可视区外：**x 593..742，而行右边界只到 570**；该行 `scrollWidth 692 / clientWidth 520`，**溢出 172px** | 我把 AutoLink 开关塞进了输入框底行（工具栏 + 高级设置 + 收费 + 提交），那一行是 `flex: 1 1; overflow: hidden` —— 空间不够时**右边的元素直接被裁掉** | 两个开关移出底行，**单独占一行**（LibTV 的开关本来也不在底行，而在输入框下方的整行区域） |
| 「自动校验素材」排版差 | 标签框 **28×36 = 三行**（「自动/校验/素材」），`white-space: normal` | 开关是 flex item，空间紧张时标签被压缩换行 | 标签 `white-space: nowrap` + 开关 `flex-shrink: 0`；现在 **72×12 一行** |

- **修的过程中我自己又踩了一个坑**：第一次把新行放在底行**内部**，它成了同一 flex 行的第三个 item，
  把设置区从 520 挤到 **218**，参数芯片与「高级设置」反而被裁。移到底行**外面**才对
  （实测：设置行溢出 172 → **0**，全面板「被裁文字」列表为空）。
- **验收**：两个开关 span `397..517` / `521..671`（间距 4px，互不重叠、都在 660 内），
  标签均为单行；面板高度 208 → 259（多出一行）。
- **顺带**：给输入框加了「贴近画布底边时整体上移」的保险（`useComposerPanel` 的 lift）。
  注意我们应用**没有** LibTV 那样的底部居中工具栏 —— 我一开始把两边的界面记混了，
  所以这道保险实际防的是画布下边缘，而不是「被工具栏盖住」。
- 截图对照：`~/artifacts/composer-before.png` / `composer-after.png`；提交 `6192d75`。

### G9. 壳层小项（每项独立） `前端` —— **已完成四项**

- **整理画布「还原 / 保留」确认（已完成）**：位置变更本来就作为**一条**历史进了撤销栈，
  但那只对知道有撤销的人有用；LibTV 的做法是直接问一次。
  **实测确认**：整理前 `translate(160px,160px)` / `(500px,160px)` → 整理后 `(0,0)` / `(470,0)`
  → 点「还原」后**精确回到 `(160,160)` / `(500,160)`**，确认条同时消失。
- **缩放档位菜单（已完成）**：点百分比弹出「放大 / 缩小 / 适合屏幕 / 缩放至 50% / 100% / 200%」。
  只放**我们自己滑杆量程内**的档位（我们 5–500，LibTV 5–800 所以最后一档是 800%），
  菜单里**不写快捷键提示** —— 我们没注册 ⌘0 这类快捷键，写了就是假提示。
  **实测确认**：点「缩放至 200%」→ 百分比与滑杆同步变成 200。
- **「AI生成」角标（已完成）**：判据是节点 data 的 `executed`（只有生成产物会置位），
  上传与裁剪都不置 → 用户自己传的图不会被标成 AI 生成。
  **实测确认（反向）**：上传的图**没有**角标（正确）；**正向未测** —— 需要真跑一次生成（花积分），
  所以「生成后确实出现角标」这条留到下次真跑时确认。
- **视频标题右侧分辨率（已完成）**：**这是推导值**。我们的生成记录只存请求的分辨率档（`720p`），
  没有产物真实像素；而 LibTV 那组数字正好等于「档位当短边、长边按比例算」（16:9 + 720P → 1280×720），
  所以按同一规则推导，认不出档位（如 `2K`）就不显示而不是瞎猜。
  **实测确认**：标题行是 `视频节点 | 1280 × 720`，与 LibTV 格式一致。
- 连线悬停剪刀按钮：`EdgeDeleteButton.vue` 已有，本轮不动（核对交互一致，无需新写）。

### G10. 不摆占位（本轮明确不做）

| 项 | 结论 | 理由 |
|---|---|---|
| 音频 / 智能剪辑 / 导演台 / 逐帧拉片 四类节点 | **不做** | 后端策略缺失；音频还卡在 TTS 上游（见 §3.0） |
| 脚本节点（新旧两版） | **不做** | 已按 B3 撤销，功能并入画布 Agent |
| 图片卡右上「…」菜单 | **暂缓** | LibTV 上只在选中+悬停时渲染，本机没能稳定复现，内容未知 |
| 「图片反推提示词」 | **暂缓** | LibTV 有；我们需要 ai-gateway 的 reverse-prompt 能力 |
| 联网搜索 / 自动校验素材 | **暂缓** | 需要真实的后端能力，先只上 AutoLink |

### 本轮验收门槛（与 §6 一致，逐条可复跑）

```bash
cd ~/CanvasMind
npm run type-check && npm run test:unit
```

外加浏览器实测三件套：尺寸断言（`offsetWidth` 对表）、输入框缩放不变断言、以及每个动作的截图。

**G10 反例记录**：LibTV 智能剪辑卡写的是「空空如也，请连接视频节点后操作」——
这是「节点自己说清缺什么」的正面写法，我们的 `node-input-rules.ts` 已经是这条路子，
将来上新节点类型时**照抄这个句式**，不要写「敬请期待」。
